# 项目问题检查清单(2026-08-13)

测试段开封后对全项目做的一次核查。按严重程度排序。

---

## 🔴 P0 — 会导致对外说错话

### 1. 多处文档仍声称"测试段未开封",现已不实

测试段已于 2026-08-12/13 正式开封(项目B 已完成,项目A 训练中)。
以下文件仍写着"自始至终未触碰 / 未开封":

`README.md`、`longonly_falsified/README.md`、`riskguard/README.md`、
`docs/简历项目简介.md`、`docs/简历与面试.md`、`docs/results_overview.md`,
以及 `项目B/results/` 下多份报告。

**处理**:结果报告里的那句是**当时的事实**,应保留但加注"(该报告生成时)";
索引类文档(README/成果总览/简历)必须改为"测试段已于 2026-08 一次性开封,
结果见 XXX",否则对外会说错。

### 2. 域分工表的依据已被自己的分析推翻,但表还没改

`docs/results_overview.md` 第一节:

> | 沪深300 / 中证500 | **项目A 纯多头选股(alpha 层)** | 多头超额 IR 1.25 / 1.22 |

这个 IR 1.25/1.22 是**未中性化**口径。项目A 的风格中性化实测:

| 域 | 原始 IR | **风格中性 IR** | 留存率 |
|---|---|---|---|
| 沪深300 | 1.53 | **0.16** | 9% |
| 中证500 | 1.36 | **0.39** | 16% |

**大盘域纯多头选股的超额九成是风格暴露**。"大盘做选股 alpha 层"这个分工
在风格中性口径下**不成立**。文档必须改写,否则是拿已被自己否掉的结论对外讲。

---

## 🟡 P1 — 口径不一致,会引起追问时露怯

### 3. 已有结果是旧市值逻辑跑的,新逻辑未回溯

`styleutil.load_mcap` 已加"最后已知股本 × 当日收盘价"的重建回落(2026-08-13),
但 A1 四域、alpha 上限扫描、稳健性检验都是**在此之前**跑的。

影响面小(那些分析都在验证段 2022-07~2024-06,市值源覆盖完整),
但**若日后重跑会得到略微不同的数字**。已在此记录,避免误认为是 bug。

### 4. 结论来源混杂(已标注,未解决)

`docs/results_overview.md` 顶部已有来源标注表:模型层五条(ML增量/GBDT同质/GRU零增量/
信息分散/重要性稳定)与多种子分布来自**前身单域(中证500)项目**,平台四域未复现。
平台目前只训了对称 LGBM(四域)+ 下尾三模型(csi1000/gz2000)。

**处理选项**:(a) 保持标注不动;(b) 在平台补跑 XGB/MLP/GRU/集成/消融。
建议 (a) —— 那些是方法论性质的结论,跨域复现概率高,补跑性价比低。

---

## 🟢 P2 — 待补强,不影响正确性

### 5. 风控信号包用的还是验证段模型

`热股池日内+日频风控项目/副项目/下尾风险预警信号/` 的信号来自
`lgbm_y5d_rank`(只到 2024-06)。现在已有 `lgbm_y5d_rank_oos_WITHTEST`
(覆盖到 2025-12-31),**信号可以延长 1.5 年**。

⚠️ 但这意味着把测试段数据也放进信号包 —— 由于测试段已开封,这不再是问题,
但要在包里注明"含测试段,不可再用于该段的样本外检验"。

### 6. 项目A 的曲线导出尚不支持测试段

`项目A/code/export_curve.py` 只有 `--neutral`,没有 `--window test`。
项目A 训练完成后需补这个参数才能出测试段曲线(项目B 的 `export_curve_b.py` 已有)。

---

## 附:本次开封的完整性核查(已通过)

| 检查 | 结果 |
|---|---|
| 配置是否开封前冻结 | ✅ `docs/frozen_test_config.md`,含预设判定标准 |
| 两个口径样本期是否一致 | ✅ 修复市值截断后均为 368/367 天 |
| 市值重建是否抬高结果 | ✅ 拆分对比:原始口径(不用市值)在后半段 IR 也上升,方向不一致 → 非伪影 |
| 是否因结果不好而改配置重跑 | ✅ 未发生,一次跑完即定稿 |
